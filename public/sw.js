/**
 * sw.js — DESACTIVADO / LEGACY
 *
 * Este archivo ya no se usa. El service worker activo es service-worker.js,
 * registrado por js/sw-register.js.
 *
 * No eliminar por compatibilidad con caches de usuarios, pero su lógica
 * está vacía para evitar conflictos con rutas inexistentes.
 */
